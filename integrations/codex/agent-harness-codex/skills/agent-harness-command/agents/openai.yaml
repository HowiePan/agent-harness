interface:
  display_name: "Agent Harness Command"
  short_description: "Resolve Harness commands, bindings, and issue intake"
  default_prompt: "Use $agent-harness-command to resolve h:engine quality TARGET, show h:where, or record h:report PROJECT."
policy:
  allow_implicit_invocation: true
