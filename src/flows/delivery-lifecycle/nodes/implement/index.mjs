import { deliveryNode } from '../node.mjs';

export const implementNode = deliveryNode('implement', 'implement', 'implementation', ['plan'], {
  acceptance: ['Execute code and asset changes in authorized paths according to feature specifications.'],
});

export const implementNodes = [implementNode];
